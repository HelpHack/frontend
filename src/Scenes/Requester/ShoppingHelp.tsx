import { StyleSheet } from "react-native";
import React from "react";

import ShoppingList from "src/Containers/Requester/ShoppingList/ShoppingList";

const ShoppingHelp = () => {
  return <ShoppingList />;
};

export default ShoppingHelp;

const styles = StyleSheet.create({});
